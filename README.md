## Team 2 Stocks Analysis

Group Members: 
- Samson Xu
- Pranav Singh
- Colby Tran
- Cristian Navarro

## Notebooks
- `notebooks/Project_Assignment_02.ipynb` : explore the data and perform basic transformations and visualizations 

## Data Setup
See `data/README.md` for download instructions.

How to setup the environment (notes):
- In the notebook environment (VS Code or Jupyter), select the `.venv` kernel created from the Quick Start.
- Python ~3.11–3.13 is suitable; install dependencies via `pip install -r requirements.txt`.

## How to run
- Open the notebook(s) and run cells top-to-bottom. Select the `.venv` Python kernel in VS Code / Jupyter so packages match `requirements.txt`.

## Dependencies
- See `requirements.txt`